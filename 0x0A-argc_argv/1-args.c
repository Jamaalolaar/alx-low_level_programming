#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/**
 * main - prints th number of arguments
 * passed to main
 * @argc: number of arguments
 * @argv: array of pointers to argument
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
