#include "main.h"

/**
 * _islower - function checks if a lowercase letter is present
 *
 * Return: postive (1), negative (0)
 */

int _islower(int c)
{
	if (c >= a && c <= z)
	{
		return (1);
	}

	else
	{
		return (0);
	}
}
