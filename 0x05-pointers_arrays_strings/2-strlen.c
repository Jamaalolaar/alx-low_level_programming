#include "main.h"
#include <stdio.h>

/**
 * _strlen - returns the lenght of a string
 * @s: argument of function
 * Return: length of string.
 */

int _strlen(char *s)
{
	int p;

	for (p = 0; s[p] != '\0'; p++)
	{
		;
	}
	return (p);
}
